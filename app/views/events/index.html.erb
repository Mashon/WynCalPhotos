<p id="notice"><%= notice %></p>

<br><br>
<div class="header">
  <p>WynCal</p>
  <br>
  <%= link_to "<i class='fa fa-at'></i>".html_safe, about_path %>
</div><br><br>


<div class="upcoming">
<center><h3>List of Upcoming Events</h3></div></center>

<!-- <table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Date/Time</th>
      <th>Location</th>
      <th colspan="4"></th>
    </tr>
  </thead>
</table> -->
    <div class="events">
    <% @events.each do |event| %>
      <%= event.name %>
      <!-- <%= event.description %> -->
      <%= event.start_date.strftime("%A, %b %d") %>
      <%= link_to 'Show', event %></p>
        <!-- <td><%= link_to 'Edit', edit_event_path(event) %></td> -->
        <!-- <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
    <% end %>
  </div>
<br>

<% if ( (@page - 1) > 0 ) %>
  <%= link_to "Previous page", page: @page - 1 %>
<% end %>

<% if @display_next %>
  <%= link_to "Next page", page: @page + 1 %>
<% end %>

<br><br><br>

<div class="newevent">
<%= link_to 'Add a New Event', new_event_path %>
</div>
